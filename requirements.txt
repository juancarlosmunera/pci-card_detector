# PCI-DSS Card Detector — optional dependencies
# Install only the connectors you need.
#
# Core tool has no dependencies (uses Python stdlib only).
# sqlite3 is included in the Python standard library.

# ── Databases ────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.0       # PostgreSQL  (--pg-host)
mysql-connector-python>=8.0  # MySQL       (--mysql-host)

# ── Document formats ─────────────────────────────────────────────────────────
pdfplumber>=0.9.0             # PDF files   (--pdf)
openpyxl>=3.1.0               # Excel files (--excel)

# ── Cloud storage ─────────────────────────────────────────────────────────────
boto3>=1.26.0                 # Amazon S3                (--s3-bucket)
google-cloud-storage>=2.0.0  # Google Cloud Storage     (--gcs-bucket)
azure-storage-blob>=12.0.0   # Azure Blob Storage       (--azure-container)
